import{describe,expect,it}from'vitest';import{buildContext,contextPercent,CONTEXT_LIMIT,createCheckpoint}from'./context';import{defaultSettings}from'./storage';import type{Conversation,Message}from'./types'
const messages:Message[]=Array.from({length:20},(_,i)=>({id:String(i),role:i%2?'assistant':'user',content:'x'.repeat(1800),createdAt:i}))
describe('context compaction',()=>{it('checkpoints long chats without deleting raw messages',()=>{const cp=createCheckpoint(messages);expect(cp).toBeTruthy();expect(messages).toHaveLength(20)});it('sends a checkpoint and recent messages',()=>{const chat:Conversation={id:'c',title:'c',messages,createdAt:0,updatedAt:0};const state=buildContext(chat,defaultSettings);expect(state.compacted).toBe(true);expect(state.messages.some(m=>m.content.includes('checkpoint'))).toBe(true);expect(chat.messages).toHaveLength(20)});it('meters the configured 28k model context',()=>{expect(CONTEXT_LIMIT).toBe(28672);expect(contextPercent(CONTEXT_LIMIT)).toBe(100)})})
