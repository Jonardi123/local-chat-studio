import { Component, type ErrorInfo, type ReactNode } from 'react'
export class ErrorBoundary extends Component<{children:ReactNode},{error:string}>{state={error:''};static getDerivedStateFromError(error:Error){return{error:error.message||String(error)}}componentDidCatch(error:Error,info:ErrorInfo){console.error('Application error',error,info)}render(){if(this.state.error)return <main className="fatal"><h1>Something went wrong</h1><p>Your saved chats are still in this browser.</p><details><summary>Technical details</summary><code>{this.state.error}</code></details><button onClick={()=>location.reload()}>Reload application</button></main>;return this.props.children}}
