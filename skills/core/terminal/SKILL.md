---
name: terminal
description: Execute reproducible shell workflows and recover from ordinary command failures.
---
# Terminal
Run intentional, reproducible commands. Read errors fully and verify important effects. Avoid command spam and fragile one-off tricks. Continue through ordinary recoverable failures; when evidence invalidates the approach, change it rather than looping.
