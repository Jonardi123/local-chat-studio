# sudoN implementation brief for local Qwen

Work autonomously in this existing React 19 + TypeScript + Vite + Electron repository. Inspect the relevant files before editing, preserve working architecture and visual language, implement code rather than describing it, and avoid unrelated rewrites.

Deliver sudoN as a compact local-AI client with: (1) a four-state composer-integrated reasoning track—Instant disables Qwen thinking, then Low, Medium, and Extra High maps to `xhigh`; (2) a small clockwise 16k active-context ring immediately left of that control; (3) non-destructive checkpoint compaction beginning near 8k tokens, retaining raw history, checkpoint, recent turns, and response space; (4) platform application-data chat persistence using append-friendly raw messages, separate versioned metadata/checkpoints/attachments, migration, search, and browser fallback; (5) chat branching from any message with immutable original, parent relationships, separate future turns and checkpoint state; (6) raw-history retrieval when the user references forgotten instructions; and (7) explicit-user-request-only named agent sessions using the existing server sequentially—never load another model—with a small temporary left floating status panel.

Use `skills/registry.json` and the focused `skills/core/*/SKILL.md` and `skills/agents/*/SKILL.md` files. Grace debugs, Ada architects, Knuth reviews, Margaret tests, Linus handles systems, and Turing researches. Do not auto-spawn agents. Preserve secrets and never grant agents model-side shell/filesystem access.

Review `src/types.ts`, `src/storage.ts`, `src/context.ts`, `src/api.ts`, `src/agents.ts`, `src/App.tsx`, `electron/main.cjs`, `electron/preload.cjs`, and existing tests first. Maintain concise checkpoints if work approaches the model context limit; files and Git are external memory. Continue through ordinary failures. Add focused tests for persistence/compaction/agent policy, then run tests, typecheck, lint, production build, and desktop packaging where available. Repair relevant failures, inspect the final diff, verify no secrets/generated output are tracked, and finish with a concise implementation and verification report.
